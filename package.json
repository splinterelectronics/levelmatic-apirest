{
  "name": "levelmatic-apirest",
  "version": "1.0.0",
  "description": "Apirest para la app de levelmatic",
  "main": "index.js",
  "scripts": {
    "start": "rimraf dist && tsc && node dist/index",
    "dev": "rimraf dist && tsc && nodemon dist/index",
    "clean": "rimraf dist && tsc",
    "compile-schemas": "json2ts -i schemas -o types",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "levelmatic",
    "iot",
    "apirest"
  ],
  "author": "Jose Valera",
  "license": "ISC",
  "dependencies": {
    "fastify": "^3.7.0"
  },
  "devDependencies": {
    "@types/node": "^14.14.6",
    "json-schema-to-typescript": "^9.1.1",
    "rimraf": "^3.0.2"
  }
}
